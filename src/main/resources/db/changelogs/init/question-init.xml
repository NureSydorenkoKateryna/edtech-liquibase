<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="002-create-question-table" author="druchek">
        <createTable tableName="question">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" primaryKeyName="pk_question" nullable="false" />
            </column>
            <column name="quiz_id" type="BIGINT">
                <constraints nullable="false" />
            </column>
            <column name="prompt" type="VARCHAR(MAX)">
                <constraints nullable="false"/>
            </column>
            <column name="question_type" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="points" type="DECIMAL(10,2)">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint baseTableName="question"
                                 baseColumnNames="quiz_id"
                                 constraintName="fk_question_quiz"
                                 referencedTableName="quiz"
                                 referencedColumnNames="id" />

        <createIndex tableName="question" indexName="idx_question_quiz_id">
            <column name="quiz_id"/>
        </createIndex>

        <sql>
            INSERT INTO dbo.tables (db_, name, type, author, version, service, description, column_count, created_at, last_update_at)
            VALUES ('liquibase_lab', 'question', 'SYSTEM', 'druchek', 1, 'testing_service',
                    'Creation of question table.',
                    (SELECT COUNT(*) FROM sys.columns WHERE object_id = OBJECT_ID('question')),
                    SYSUTCDATETIME(), NULL);
        </sql>

        <rollback>
            <sql>DELETE FROM dbo.tables WHERE name = 'question';</sql>
            <dropTable tableName="question"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
