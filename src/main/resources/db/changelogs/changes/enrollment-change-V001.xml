<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="013-add-completed-lessons-to-enrollment" author="sydorenko">
        <preConditions onFail="MARK_RAN">
            <tableExists tableName="enrollment"/>
            <not>
                <columnExists tableName="enrollment" columnName="completed_lessons_count"/>
            </not>
        </preConditions>

        <addColumn tableName="enrollment">
            <column name="completed_lessons_count" type="INT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
        </addColumn>

        <sql>
            INSERT INTO dbo.tables ( db_, name, type, author, version, service, description, column_count, created_at, last_update_at )
            VALUES (
            'liquibase_lab',
            'enrollment',
            'SYSTEM',
            'sydorenko',
            (SELECT COALESCE(MAX(version), 0) + 1 FROM dbo.tables WHERE name = 'enrollment' AND db_ = 'liquibase_lab'),
            'student_progress_service',
            'Added completed_lessons_count',
            (SELECT COUNT(*) FROM sys.columns WHERE object_id = OBJECT_ID('enrollment')),
            SYSUTCDATETIME(),
            SYSUTCDATETIME());
        </sql>

        <rollback>
            <dropColumn tableName="enrollment" columnName="completed_lessons_count"/>
            <sql>
                DELETE FROM dbo.tables
                WHERE name = 'enrollment'
                AND db_ = 'liquibase_lab'
                AND version = (SELECT MAX(version) FROM dbo.tables WHERE name = 'enrollment' AND db_ = 'liquibase_lab');
            </sql>
        </rollback>
    </changeSet>
</databaseChangeLog>
